name: sendex_app
description: Sendex BLE GPS tracker
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.4
  pdf: ^3.11.1
  printing: ^5.13.4

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
